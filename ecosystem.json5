{
  "apps": [{
    "name"  : "gps-track",
    "script": "yarn",
    "args": "server"
  }],
  "deploy": {
    "production": {
      "user"       : "me",
      "host"       : "balthazargronon.com",
      "ref"        : "origin/master",
      "repo"       : "git@github.com:balthazar/gps-track.git",
      "path"       : "/var/www/gps-track",
      "post-deploy": "yarn && pm2 startOrRestart ecosystem.json5 --env production --log-date-format 'DD-MM HH:mm:ss.SSS'",
    }
  }
}
